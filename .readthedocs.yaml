version: "2"

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  apt_packages:
    - libgdal-dev
    - gdal-bin
    - python-gdal
python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
