version: "2"

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  apt_packages:
    - libgdal-dev
    - gdal-bin
    - python3-gdal
python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .

sphinx:
  configuration: docs/source/conf.py
